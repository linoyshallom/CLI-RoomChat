# CLI--RoomChat
in order to run the program : 
- activate venv
- run `python run_servers.py` to start both server_chat.py and server_file_transfer.py together (Ctrl+C stops both)
- run client (important to run by cmd)
